// import React, { lazy, Suspense } from 'react';

// const LazySong = lazy(() => import('./Song'));

// const Song = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
//   <Suspense fallback={null}>
//     <LazySong {...props} />
//   </Suspense>
// );

// export default Song;
// #TODO: fix lazy loading
