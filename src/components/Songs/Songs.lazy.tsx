// import React, { lazy, Suspense } from 'react';

// const LazySongs = lazy(() => import('./Songs'));

// const Songs = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
//   <Suspense fallback={null}>
//     <LazySongs {...props} />
//   </Suspense>
// );

// export default Songs;
